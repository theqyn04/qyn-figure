@model IEnumerable<BrandModel>

@foreach (var item in Model)
{
	<div class="panel panel-default">
		<div class="panel-heading">
			<h4 class="panel-title"><a asp-controller="Brand" asp-action="Index" asp-route-Name="@item.Name">@item.Name</a></h4>
		</div>
	</div>
}